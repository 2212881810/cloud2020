server:
  port: 7001
eureka:
  instance:
    hostname: eureka7001.com  # 本机eureka服务实例名称
  client:
   # false 不向注册中心注册自己
    register-with-eureka: false
   # 表示不需要检索服务,检索服务是客户端干的事
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka   # 注册到7002上面去


