server:
  port: 7002
eureka:
  instance:
    hostname: eureka7002.com  # 本机eureka服务实例名称
  client:
   # false 不向注册中心注册自己
    register-with-eureka: false
   # 表示不需要检索服务,检索服务是客户端干的事
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka


